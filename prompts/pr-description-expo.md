You are generating the PR description for an auto-sync PR opened by `clevertap-wrapper-sync` for the **CleverTap Expo plugin**.

## Inputs

- **Release name:** ${RELEASE_NAME}
- **Branch:** ${BRANCH}
- **Target clevertap-react-native version:** ${CLEVERTAP_RN_VERSION}
- **Target Expo SDK version:** ${EXPO_SDK_VERSION}

The Expo sync log (the `claude -p --output-format json` envelope) is **appended verbatim at the END of this prompt**, between `=== BEGIN ... ===` / `=== END ... ===` markers. The envelope's `.result` field contains the structured sync log as JSON (`version_bumps`, `integration_steps_changed`, `build_propagated`, `flagged_for_review`, `compatibility_matrix_row`, `version_bump`, `docs_updated`, `native_changelogs`, etc.); the envelope's top level has `total_cost_usd` and `usage`. Parse those. Use only the data present — do not invent or speculate.

Remember this is a CONFIG PLUGIN: it does NOT surface SDK methods. The PR is about version bumps and native-integration-step changes, not new wrapped APIs.

## Output

A single Markdown document, written to stdout. The CI captures it and uses it as the PR body. Format:

```markdown
## Sync expo ← clevertap-react-native ${CLEVERTAP_RN_VERSION} + Expo SDK ${EXPO_SDK_VERSION} (${RELEASE_NAME})

🤖 Auto-generated by `clevertap-wrapper-sync` via `clevertap-wrapper-tooling`.

### ⚠️ Flagged for review — NOT auto-applied

Render this section ONLY if `flagged_for_review` is non-empty; otherwise omit it
entirely. These are changes the automation could not safely apply on its own — a
human must decide. Keep it at the TOP so it isn't missed. One bullet per item:

- ❓ **<type>**: `<name>` — <rationale>.

(types: `ambiguous_mapping`, `plugin_only`, `behavior`, `unconfirmed`, `expo_breaking`)

### Resolved dependency chain

From `resolved_chain`: clevertap-react-native ${CLEVERTAP_RN_VERSION} requires CleverTap Android SDK `<required_android_core>`, iOS SDK `<required_ios_core>`, push-templates `<required_push_templates>`, HMS `<required_hms>`.

### Version bumps applied

One bullet per `version_bumps` entry: `\`<key>\`: <from> → <to> (\`<file>\`)`. If an entry has `"source_verified": false`, append ` — ⚠️ NOT independently verified; reviewer must confirm`.

### Integration steps changed

One bullet per `integration_steps_changed` entry: `<change> (\`<files>\`)`. Omit the section if empty.

### Build config propagated

One bullet per `build_propagated` entry (e.g. iOS deployment-target / minSdk bumps). Omit if empty.

### New APIs showcased in CTExample

One bullet per `apis_demoed` entry: `\`<method>()\` — demo added under <category> (\`CTExample/App.js\`)`. These are new clevertap-react-native APIs wired as runnable demo buttons in the example app (JS only — the plugin surfaces no methods). Omit the section if `apis_demoed` is empty.

### Release prep

- **Plugin version:** <version_bump.from> → <version_bump.to> (<version_bump.bump_type> — <version_bump.rationale>)
- **Compatibility matrix row added:** `| <plugin_version> | <expo_sdk> | <react_native> | <clevertap_rn> |` (from `compatibility_matrix_row`)
- **Docs updated:** <comma-separated `docs_updated` entries>

### Native CHANGELOG entries (reviewer reference)

Pull from the sync log's `native_changelogs`. For **`rn`**, **`android_core`**, and **`ios_core`**, render the FULL changelog — the `target_entry` PLUS **every** `intermediate_entries[].entry` — verbatim, each SDK wrapped in a **collapsible** `<details>` block so the PR stays scannable. Also include `push_templates` / `hms` in their own `<details>` if their `target_entry` is non-empty. **Expo stays as links only** (no full text — it would bloat the PR).

Exact shape — emit raw HTML `<details>` (GitHub renders the markdown inside; keep the blank lines):

```markdown
<details>
<summary><b>clevertap-react-native</b> — full changelog (target <rn.target_version> + N intermediate version(s))</summary>

<verbatim rn.target_entry>

<verbatim rn.intermediate_entries[0].entry>

<verbatim rn.intermediate_entries[1].entry>
...
</details>

<details>
<summary><b>CleverTap Android SDK</b> — full changelog (target <android_core.target_version> + N intermediate version(s))</summary>

<verbatim android_core.target_entry>

<verbatim each android_core.intermediate_entries[].entry>
</details>

<details>
<summary><b>CleverTap iOS SDK</b> — full changelog (target <ios_core.target_version> + N intermediate version(s))</summary>

<verbatim ios_core.target_entry>

<verbatim each ios_core.intermediate_entries[].entry>
</details>
```

Then (if present) the same `<details>` shape for `push_templates` and `hms` (target_entry only). Finally, Expo as a single line (links only):

`**Expo SDK ${EXPO_SDK_VERSION}:** <native_changelogs.expo.summary> — see [release notes](<sources[0]>) and [GitHub CHANGELOG](<sources[1]>).`

Rules: paste each `entry` VERBATIM (do not summarize or trim). Count intermediates accurately in each `<summary>`. If an SDK block is absent/empty in `native_changelogs`, omit its `<details>`. If `native_changelogs` is missing entirely, skip this whole section — don't render a stub.

### Run metadata

- Model: <from claude output>
- Tokens used: <usage input+output>
- Approx cost: $<total_cost_usd>
- Soft cap ($3) exceeded: yes/no

### For the reviewer

This PR is auto-generated. Please:

1. Read **Flagged for review** first — these need a human decision (especially `plugin_only` / `ambiguous_mapping` version bumps that were deliberately NOT auto-applied).
2. Check **Version bumps** — confirm each is the right artifact and version (the fact-finder matched by Maven coordinate; verify the CleverTap SDK versions match what clevertap-react-native ${CLEVERTAP_RN_VERSION} ships).
3. Check **Integration steps / Build config** — any new permission/repo/pod or minSdk/deployment-target bump is host-facing; make sure CHANGELOG calls breaking ones out.
4. CI must be green before merge: the CTExample app must compile (Android gradle + iOS xcodebuild) after `expo prebuild`.
```

## Constraints

- **Output ONLY the Markdown PR body.** Your entire response must begin with the `## Sync expo ...` heading. No preamble, no reasoning, and do NOT wrap the whole document in a ```` ```markdown ```` fence.
- **Don't invent data.** Omit a section rather than render a placeholder.
- **Keep it short.** Bullets are one line each. No paragraphs.
- **Match the schema exactly** — downstream tooling may parse the headings.
